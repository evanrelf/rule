{-# LANGUAGE NoImplicitPrelude #-}

module Rule.Do
  ( fmap
  , (<*>)
  , (>>=)
  , (>>)
  , join
  )
where

-- https://ghc.gitlab.haskell.org/ghc/doc/users_guide/exts/qualified_do.html

fmap :: ()
fmap = ()

(<*>) :: ()
(<*>) = ()

(>>=) :: ()
(>>=) = ()

(>>) :: ()
(>>) = ()

join :: ()
join = ()
